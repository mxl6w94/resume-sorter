{
  "name": "resume-sorter",
  "version": "0.0.1",
  "private": true,
  "description": "Client-side applicant ranking prototype. Static HTML + Firebase + Gemini. No build step.",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/unit/scoring.test.mjs tests/unit/errors.test.mjs",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "serve": "npx http-server -p 5173 -c-1 --silent"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "http-server": "^14.1.1"
  }
}
